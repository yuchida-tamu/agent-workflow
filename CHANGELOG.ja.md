# 変更履歴（Changelog）

`agent-workflow` の注目すべき変更点を、コミット単位ではなく能力（capability）
領域ごとに記録します。これは履歴を追体験したい貢献者ではなく、導入するかどうかを
判断する利用者に向けて書かれています。設計については `docs/agent-loop-architecture.html`
（日本語版: `docs/agent-loop-architecture.ja.html`）を、この文書が要約している
ビルド状況の台帳については `STATUS.md` を参照してください。

> この文書は英語版 `CHANGELOG.md`（`b75f088` 時点）の内容に基づいています。

## v0.4.0 — ヘッドレスのループが、ようやく閉じます

（以下はすべて v0.3.0 以降の新規項目です。）

v0.3.0 は、ヘッドレスのディスパッチとレビューを、切り替え可能な段階として
出荷しました。それを実際の利用者リポジトリ（`hsk-habit`）で有効にしたところ、
どの段階も成果物を生成したうえで、それを捨てる・拒否する・誤った課題に
向ける、のいずれかをしていることが分かりました。利用者側から見える不具合が
5 件、すべてそのリポジトリから報告され、すべてここで修正されています。
`headless.*` を有効にしたまま `@v0.3.0` をピン留めしている利用者が抱えて
いるのは、動きはするが着地しないループです。このタグが、それを閉じます。

### ヘッドレスレビュー — 判定が、推測ではなく読まれるようになりました

`findingsFromText` は、エージェントの出力**全体**を `JSON.parse` して
いました。code-reviewer は、指摘（findings）を ```json のフェンス付き
ブロックに入れた散文を出力します。そのため parse は常に例外を投げ、
`findings` は常に `null` になり、`verdictFromFindings` は（それ自体は
正しい）「読めないものは拒否する」という既定により `not-mergeable` を
返していました。ヘッドレスレビューは、何を見つけたかに関わらず、すべて
拒否していたのです — 問題のないレビューと、マージを止めるべきレビューが、
バイト単位で同一でした（#171、#183 で修正）。

現在は、フェンス付きブロックを走査して結論部の `findings` を読み取る経路に
フォールバックするため、判定はエージェントの実際の指摘に従います。

同じ実行が、呼び出しごとにネイティブの `CHANGES_REQUESTED` を提出し、直前の
ものを一度も整理しませんでした。その結果、PR には解除されないブロッキング
レビューが積み上がり — 報告元のリポジトリでは 1 つの PR に 2 件ずつ —
指摘が 1 件もない場合でも本文は「指摘はレビュー成果物のコメントにあります」と
主張していました（#181、同じく #183）。ネイティブレビューは、追加ではなく
整理されるようになりました。

### post-merge — 実行できないスモークが、マージを放棄せず縮退します

パックの探索が、`packs/` を**利用者側**のチェックアウトに対して解決して
いました。そこには誰もパックを配置しません。`adopt` はパックを導入せず、
そうせよと書いた文書もありませんでした。シナリオが存在すると、この段階は
`process.exit(20)` に到達します — しかもそれは、状態遷移を適用しスモークの
注記を投稿するループの**手前**でした。紐づいた課題は CLOSED のまま
マージ前のラベルで取り残され、`main` はマージのたびに赤くなりました
（#182、#191 で修正）。

これは、実際に課題をクローズする最初の PR まで表に出ませんでした。それ以前の
マージは、もっと手前の `PR closes no issue — nothing to transition` で
抜けていたためです。つまりリポジトリは何度マージしても健全に見え、ループを
意図どおりに使った最初の 1 件で壊れます。

`resolvePackDir` は、パックが実際に置かれているツールキット側を解決するように
なりました。また、実行できないスモークは `smokeSkipped` に縮退し、注記に
正直に記録されたうえで、マージ側の記帳は適用されます。**本当に失敗した
リプレイは、これまでどおりマージを止めます。**

### ディスパッチ — 親課題が実装者を起動しなくなりました

親課題に `state:ready` が付くと、それ自身には作業のない親に対して実装者が
ディスパッチされていました。`agentflow-next` はまさにこのために
`PARENT_WAITING` ガードを持っていましたが、ラベル起動のディスパッチャは
`childrenOf` を一度も参照していませんでした（#180、#186 で修正）。現在は
参照します。

`fcd18d4`（#190）は、再計画によって孤立した子課題を、旧計画に黙って
ぶら下げたままにせず表面化させます。

### 既知の欠落 — ヘッドレス実装者は、まだ書き込めません

親課題ディスパッチの不具合と併せて報告されましたが、ここでは**修正されて
いません**。`DEFAULT_ALLOWED_TOOLS` は `["Read", "Grep", "Glob"]` で、
`--permission-mode plan` とともに全ロールへ一律に適用されます。architect と
code-reviewer には正しく、書くことが仕事である実装者には致命的です。
`launchPlan` は `allowedTools` の上書きを受け取れますが、それを渡す箇所が
ありません。`agents/implementer.md` がエージェントに期待させているワーク
ツリーを用意する処理も、どこにもありません。

したがって `headless.dispatch.ready` は、実行を 1 回消費して、行き詰まりを
報告する成果物を投稿します。エージェントは安全側に倒れ、何があれば進めるかを
正確に述べますが、この段階は成功しえません。これが入るまで `ready` は
無効のままにしてください。

### その他

- **状態遷移が冪等な compare-and-swap** になりました（#172）。ドライバを
  またいでも、再適用が項目を二重に進めることはありません。
- **PR のリスク判定が、マップ外のコードを** 重大度に算入し、マップの腐敗を
  警告します（#169）。マップ外の差分表面が、無害であるかのように採点される
  ことはなくなりました。
- **`agentflow-gates`** が、承認の受信箱に正面玄関を与えます（#173）。
- **`release --verify`** が、`HEAD` ではなく項目ごとのバージョンを検査します
  （#170）。
- **ロックの奪取**が、調停ファイルなしの inode 検査による確保になりました
  （#184）。アダプターの残留物には、ロックの経年失効と pid に紐づく準備完了
  判定が入りました（#177）。
- **pack-expo の `ship`** に仕様が書かれました（#176）。実装はまだです。
- **日本語のハウス用語集**が、サイトと文書で統一されました（#174）。

## v0.3.0 — プラットフォームの柱

（以下はすべて v0.2.0 以降の新規項目です。v0.2.0 までに出荷済みの内容は、
その節を参照してください。）

このリポジトリは公開され、MIT ライセンスになり、ドキュメントサイトを持つに
至りました。そして目玉として、`pack-expo` の `run`/`verify`/`execute-step`
アダプターは、実機で起動した iOS シミュレータに対してライブで証明済みです —
マージ済み・ユニットテスト済みというだけではありません。`@v0.2.0` を
ピン留めしている利用者は、この 3 つのアダプターに加えて、ヘッドレス
ディスパッチの実運用上のバグも抱えたままでした。このタグは、その両方を
埋めます。

### pack-expo アダプター — マージ済みではなく、ライブで証明済み

`run`・`verify`・`execute-step` は実装され（#138, #140, #141）、その後、
実機で起動した実際の Expo アプリに対する 4 ラウンドのライブ受け入れ
キャンペーン（#159）にかけられました。provision → start → verify →
execute-step → stop、モックなしです。各ラウンドは、直前のラウンドが
見つけた問題を解消しながら進みました。このキャンペーンこそが、この
パックを「マージ済み」ではなく「証明済み」と呼べる理由です。

- **#156**（P0）— `spawnBackground` が、まだ開いていない `WriteStream` を
  `spawn()` に渡していました。実機での `run start` は、テストしたすべての
  Node バージョンで、決定論的に即座に例外を投げていました。ログ用の fd を
  同期的に開くことで修正しました。
- **#158**（P1）— 再利用の高速経路が、agent-device の実際の `"DisplayName
  (bundle.id)"` 形式のアプリ一覧に一致しませんでした。そのため、開発
  クライアントがすでにインストール済みでも、`run start` のたびに数分がかり
  の再ビルドが発生していました。
- **#162**（P0）— `run start` が、汎用の Expo Go スキーム（`exp://…`）で
  開発クライアントを開いていました。Expo Go も入っている環境では OS の
  選択ダイアログに阻まれるか、まったく別のアプリが開いてしまいました。現在
  は、Expo 自身の CLI が使うのと同じ、バンドルスコープの
  `expo-development-client` ディープリンクを組み立てます。
- **#164**（P0）— `execute-step` の `text` アサーションが、エンベロープを
  外さないまま生のレスポンスを読んでいたため、常に空文字を読んでいました。
  構造的に修正しました。アンラップ処理を、すべてのアダプター呼び出しが
  必ず通る単一の合流点 `invoke()` 自体に移したため、今後どの呼び出し元も
  同じ種類のバグを再発させることができません。

4 件すべてクローズ済みで、ライブで再検証済みです。キャンペーンの最終
ラウンドは、端から端まできれいに通過しました。`ship` アダプター（EAS の
ビルド・配布・提出）は、まだ仕様のみです（#137）。これが着地するまでは、
プラットフォーム対応を「ビルドを実行・検証できる」であって「ビルドして
出荷できる」ではない、として扱ってください。

### ヘッドレスディスパッチ — 成功した実行の成果物が失われなくなりました

`headless.dispatch.<state>` には、実運用で見つかった利用者向けのバグが
ありました（#157、hsk-habit が起票）。完了した実行が台帳の行だけを投稿し、
すでに生成していた成果物を黙って捨てていたのです — 成功は、何も出力
しなかった実行とまったく同じに見えていました。#160 で修正しました。成功
した実行は今、台帳やリスク判定がすでに使っているのと同じマーカー管理の
追記パターンで、状態ごとの永続的なマーカーの下に成果物を投稿します。
`headless.dispatch` は（このリポジトリ自身も含め）既定ではすべての状態で
オフのままです。この修正は、それを有効化した利用者側のギャップを埋める
ものです。

### リポジトリの公開・MIT ライセンス化・ドキュメントサイト

リポジトリは公開され、MIT ライセンスになりました（#146）。バイリンガル
（EN/JA）の対話的なドキュメントサイトが
[yuchida-tamu.github.io/agent-workflow](https://yuchida-tamu.github.io/agent-workflow/)
で公開されています（#145）。サイト自体も、機械翻訳ではなくネイティブな
日本語で書き直されています（#151）。`README.ja.md`・
`docs/getting-started.ja.md`・`CHANGELOG.ja.md`・
`agent-loop-architecture.ja.html` — 4 つの中核文書の完全な日本語版で、
いずれもネイティブな技術文書として書かれています（#150）— が、英語版と
双方向にリンクされて加わりました。外部への文書リンクは、生のソース blob
ではなく、レンダリング済みの Pages の行き先を指すようになりました（#154。
そのレビューの続きとして、フロントマターの引用符の修正も含みます、
#155）。

### v0.3.0 時点で分かっている欠落

- **`pack-expo` の `ship` アダプターは、仕様化されているが未出荷です。**
  `run`・`verify`・`execute-step` は上記のとおりライブで証明済みですが、
  `ship`（EAS のビルド・配布・提出）は `interfaces/` にインターフェースが
  あるのみです。これが次のマイルストーンです。
- `headless.dispatch.<state>` は、作られてテスト済みで、成果物も永続化
  されるようになりましたが、このリポジトリ自身も含め、既定ではまだどの
  状態でも有効化されていません。
- 夜間 QA（スケジュールされたヘッドレス探索）は、コストの都合で保留して
  います。セルフホストの macOS ランナーが必要です。

[Unreleased]: https://github.com/yuchida-tamu/agent-workflow/compare/v0.3.0...HEAD
[v0.3.0]: https://github.com/yuchida-tamu/agent-workflow/releases/tag/v0.3.0

## v0.2.0 — 最初の完全なリリース

（以下はすべて、このタグ時点でのツールキットについての記述です。）

利用側のリポジトリが `@main` に乗るのではなく、ピン留めできる最初のバージョン
です。決定論的なコア、エージェントの陣容、App アイデンティティ、ヘッドレス
レビューは出荷済みで、このリポジトリ自身のバックログを動かしています。一方で
プラットフォームパック（pack-expo のアダプター）は仕様化されているものの未実装で、
インターフェースに対して実際のパックが出荷されるまでは実験的なままです。

### ループのコア — ステートマシン、ゲート、ディスパッチ

作業アイテムのライフサイクル（`idea → spec → planned → ready → in-progress →
in-review → merged → verified → released`）は `state:*` ラベルとして強制され、
G1（ブリーフ）、G2（計画、リスクベース）、G3（マージ）、G4（リリース）で
ゲートされた遷移を伴います。ステートマシンそのものは `agentflow-state` が所有し、
`agentflow-gate` は、いかなる遷移が適用される前にも、`/approve [gate]` と
`/reject` の Issue コメントを承認権限のある承認者リストに照らして検証します。
`agentflow-next` はクロールフェーズのディスパッチャで、リポジトリを与えると、
次に誰が、なぜ動くべきかを示します。これによってループは、コマンド 1 つずつで
駆動することも、イベントに配線することもできます。親↔子の構造は、利用可能な
ところでは GitHub のネイティブなサブ Issue に乗り、フォールバックとして本文の
`Child of #N` 行を用います。依存関係のある子は `integrate/<topic>` ブランチに
積み重ね、1 つの統合 PR を通じて着地できます。

### リスクポリシーエンジン + パック

純粋なルールエンジン（`agentflow-policy`）が、diff・ドメイン・ビジネスの事実を
判定へと変えます。判定はスコア、レベル、そして一連の帰結（ゲートの要求、自動
マージのブロック、追加シナリオの実行、誰かへの通知）から成り、監査可能な要因
ごとのコメントとして投稿されます。ハードトリガー、加算的なスコア要因、ビジネス
ルールは同じルール形状を共有します。そのため、プロジェクト自身の
`policies/business.yml` は、プラットフォーム中立な `policies/baseline.yaml` と、
特別扱いなしで合成されます。ポリシーパックは埋め込みのフィクスチャテストを持ち、
`agentflow-policy test` で実行できます。

### 事実抽出 + ドリフト検出

`agentflow-facts` は、git の範囲を、ポリシーエンジンが読む事実へと変えます。
`diff.*`（サイズ、ファイル種別）、`domains.*`（ビジネス上の重要度、`domains.yml`
経由）、`drift.*`（承認済み計画に対するスコープ、ブリーフに対するドメイン
フットプリント）です。未対応づけのコードは、コアにハードコードされた閾値を持ち
ません。`unmapped_criticality` はプロジェクトごとのオプトインの config 値です。
ただしインストーラーは出発点を出荷します。`unmapped_warn_fraction`
（テンプレート既定値 `0.2`）は `agentflow-init adopt --coverage` がすでに強制する
config 値で、リポジトリの未対応づけの割合が、自分で決めた予算を超えたときに警告
します。

### 実行台帳 + 監査

すべての作業アイテムは、マーカー管理された台帳コメントを 1 つ持ちます
（`agentflow-log start` / `end`）。その人間可読なテーブルが、それ自体のストレージ
です。人間が読むものと、`agentflow-log audit` がパースするものは、同じバイト列
です。`audit` は各行のモデルを、そのエージェント定義が宣言するティアと突き合わせ、
フェーズの欠落や未知のエージェントを検出します。また `--since` を取り、台帳より
前に作られた Issue を除外します。そうしないと、それらについて永続的な偽の欠落を
報告してしまうからです。

### レビューガード — 機械的にスキップ不能なレビュー

独立したレビューは、いまや慣行ではなくコードで強制されています。レビュー成果物の
リーダー、発行ステップ（レビュアーが、レビューした正確な head SHA を明記した
マーカー管理コメントを、G3 の両モードで投稿する）、そして信頼できるレビュアーの
アイデンティティで絞り込んでから「最新の」判定へと畳み込む合成レイヤー。これらが
単一のガード `reviewAuthorises` に流れ込みます。このガードは、マシンが G3 の結果を
発行しうる 2 か所、すなわち `validateApproval` と自動マージの判断の両方に組み込ま
れます。現在の head の、新鮮でマージ可能なレビューを欠く PR は、両方の経路で安全側
に倒れて失敗します。同じガードは、古いレビューが、後続の未レビューのコミットに
対する承認としてリプレイされることも防ぎます。

### App アイデンティティ + G3 モード

エージェントは、メンテナー自身のアカウントではなく、GitHub App として振る舞える
ようになりました。`agentflow-identity` が、依存ゼロ（`node:crypto` + `fetch`）で
インストールトークンを発行し、あらゆる複合 Action が App の資格情報を、
`GITHUB_TOKEN` への目立つフォールバックとともに通します。これによって、リポジトリ
が 2 つの正当な G3 モードのどちらにあるかが決まり、`adopt --verify` が報告します。
`native-review`（App が作成したエージェント PR、本物の承認レビュー）か、
`solo-comment`（人間自身の PR、SHA を明記した `/approve` コメントとその後の
マージ。GitHub はそれ以外の方法での自己レビューを禁じます）です。ボットが作成した
ゲート承認は、承認者リストが参照される前に、コードで拒否されます。例外はただ 1 つ、
狭く監査されたもので、記録済みのリスク判定がすでに無人マージを承認している PR に
対する G3 です。そこでは App は、判定を発行するのではなく、エンジンの決定を
書き写します。`/approve G3` を *Issue* コメントとして投稿すると、構造上つねに
拒否されます。G3 は PR 上にあり、Issue は PR がマージされたときに、マージ後の自動
処理を通じて進むからです。

### ヘッドレスなステージ

GitHub のイベントは、対話的なセッションなしのランナー上で、一度に 1 ステージずつ
エージェントを起動できます。あらゆるフラグは既定でオフの状態で出荷されます。
`headless.review` は稼働中です。`pull_request` が code-reviewer をヘッドレスに、
App が作成者として起動します。`headless.dispatch.<state>`（状態ラベルごとに、指定
されたエージェントを起動する）は、作られてテスト済みですが、どの状態にもまだ
有効化されていません。ヘッドレス実行の課金は `CLAUDE_CODE_OAUTH_TOKEN` を通じて
Claude のサブスクリプションに対してのみ行われます。`ANTHROPIC_API_KEY` は、単に
使われないのではなく、設計上、子プロセスの環境から取り除かれます。セットアップと
トークンのローテーションについては `docs/headless-runbook.md`（英語のみ）を参照
してください。

### 入口の経路 — genesis、adopt、init のツール群

ループへの入口は 2 通りです。`project-genesis`（Opus）は、まったく新しい
プロジェクトの経路です。一度ヒアリングを行い、リポジトリ、`CLAUDE.md`、そして
初期 Issue を投入したマイルストーン 1 のバックログを、一度のパスで用意します。
`agentflow-init adopt` は、既存リポジトリの経路です。追加的かつ冪等で、18 個の
ラベル一式のうち欠けているものを作成し、config・`domains.yml`・初期のビジネス
パック・`e2e/` ディレクトリの雛形を生成し、エージェントの陣容を `.claude/agents/` に
設置し、ループが必要とするリポジトリ設定について `gh api` コマンドを出力します
（適用は決してしません）。`--verify` は実際に反映されたものを、チェックごとに
再読込します。`--coverage` は、追跡対象のソースのうち `domains.yml` が分類している
割合を報告します。

### E2E ランナーのコア + コンパイル&リプレイの仕様

Gherkin のパーサーとトレースリプレイランナー（`agentflow-e2e run` / `smoke`）は、
`.feature` シナリオを一度コンパイルし、コンパイル済みのトレースを、プラットフォーム
の `execute-step` アダプターに対してリプレイします。タグで選択でき、終了コードで駆動
されます（0 正常 · 10 回復可能 · 20 致命的）。この上に、境界付きリトライの
スクリプトが乗ります。文法とトレース形式は `scenarios/SPEC.md` に仕様化されて
います。マージ後には `agentflow-e2e smoke` がリポジトリ自身のスイートに対して実行
され、空のスイートでは何も検証せずに通ります。これはランナーの失敗ではなく、その
リポジトリのカバレッジについての事実です。そのため、ループの末尾は、アプリだけ
でなく、ツールキットやライブラリでも完了します。

### v0.2.0 時点で分かっている欠落

- **`pack-expo` のアダプターは、仕様化されているが未出荷。** 4 つのインター
  フェース（`run`・`verify`・`execute-step`・`ship`）は `interfaces/` に確定して
  おり、`packs/expo/policies/expo.yaml` はフィクスチャテスト付きで出荷されて
  いますが、アダプターの実装はまだ存在しません。パックがそれらのインターフェース
  に対して着地するまで、プラットフォーム対応は実験的なものとして扱ってください。
  これが次のマイルストーンです。
- 夜間 QA（スケジュールされたヘッドレス探索）は、コストの都合で保留しています。
  セルフホストの macOS ランナーが必要です。
- ループ全体を端から端まで走らせた利用側アプリは、まだありません。このリポジトリ
  が、これまでのところ唯一の本番利用者です。

[v0.2.0]: https://github.com/yuchida-tamu/agent-workflow/releases/tag/v0.2.0

## v0.1.0 — 歴史的な記録

`agentflow-release` そのものを構築・実地テストしている最中に、開発の途中で切られた
バージョンです（2026-07-26、コミット `794667b`）。動作はするが不完全なスナップ
ショットで、レビューガード、スカッシュ対応の配送チェック、リリースのドキュメント
より前のものです。タグはここではポリシー上イミュータブルであるため、残してあり
ます。リリース CLI は公開済みのバージョンを指し直すことを拒みますし、それが強制
することを、私たち自身も実践しています。
